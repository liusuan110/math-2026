# 问题三：733 点数据固化与复核

本记录完成问题三第七个最小步骤：把已通过完整 40 周期验证的主壳体响应固化为可复现数据文件，并完成全量回读核验。本步没有写入官方 Excel 模板。

## 1. 固化文件

- `results/models/q3_full_response.npz`：压缩二进制数据，包含时间、主积分状态、矩阵指数状态和状态名；
- `results/models/q3_full_response.json`：跨平台可读版本，包含完整 733 点时间与八状态、模型口径、求解器设置、关键行和 NPZ 文件指纹；
- `results/models/q3_data_freeze_validation.json`：固化数据回读验证结果。

数据规模为：

\[
733\ \text{个时刻}\times 8\ \text{个状态}.
\]

状态列顺序固定为：

1. 浮子垂荡位移；
2. 浮子垂荡速度；
3. 振子垂荡位移；
4. 振子垂荡速度；
5. 浮子纵摇角位移；
6. 浮子纵摇角速度；
7. 振子纵摇角位移；
8. 振子纵摇角速度。

## 2. 文件完整性

| 文件 | 大小 | SHA-256 |
|---|---:|---|
| `q3_full_response.npz` | 92110 bytes | `dfb003908a6909fd09eaebf25c31a40b05e4c9057e03421c2b50517361830b62` |
| `q3_full_response.json` | 191937 bytes | `5a400b37a30cc09fd03b8a0d51737498cbc1cf763e8921c9704f67690a3b29d2` |
| `q3_full_validation.json` | — | `54dd3489fa4d01cd1cfa0dd6cda713780c25597d103287dca1a1ca4872792b02` |

JSON 清单内部记录 NPZ 指纹，回读测试重新计算后完全一致。

## 3. 固化前独立复核

固化脚本重新运行主积分，并在全部 733 个官方时刻与矩阵指数精确瞬态解比较：

\[
\varepsilon_{\max,\mathrm{norm}}
=6.5068\times10^{-14}
<5\times10^{-8}.
\]

通过后才允许数据落盘。

## 4. 回读测试

`verify_q3_frozen_data.py` 最终结果为 **10/10 通过**：

1. 三个来源/数据文件存在且完整验证状态为 passed；
2. 数据形状为 \((733,8)\)，状态顺序正确且全部为有限数值；
3. 时间列与重新构造的官方 0.2 s 网格逐值一致；
4. JSON 与 NPZ 的时间、状态数组逐值一致；
5. NPZ 文件指纹与 JSON 清单一致；
6. 已存矩阵指数数组与重新计算结果一致；
7. 已存主积分数组与重新积分结果一致；
8. 主积分与矩阵指数全网格误差通过；
9. 10、20、40、60、100 s 行与 JSON 摘要及完整验证报告一致；
10. 0 s 初始八状态严格为零。

测试结果：0 个失败，0 个错误。

## 5. 关键行索引

若数据区从零开始计数，五个论文关键时刻的索引为：

| 时间（s） | 零基索引 | 写入 Excel 后的工作表行号 |
|---:|---:|---:|
| 10 | 50 | 53 |
| 20 | 100 | 103 |
| 40 | 200 | 203 |
| 60 | 300 | 303 |
| 100 | 500 | 503 |

Excel 行号按两行表头、数据从第 3 行开始计算。这些行将在工作簿写入后再次逐单元格核验。

## 6. 本步结论与下一检查点

733 点主模型时序数据已经固化，且通过重新积分、矩阵指数、双格式回读、文件指纹和关键行五层复核，可以作为唯一的 `result3.xlsx` 数据源。

下一步只将该固化数据写入官方空白模板，保留原两行表头和九列顺序；随后重新打开文件，逐单元格比较全部 733×9 个数据单元格，并进行工作簿视觉渲染检查。此步骤暂不生成论文正式图。
