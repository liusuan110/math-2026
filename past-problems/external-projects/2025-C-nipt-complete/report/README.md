# 国赛 LaTeX 模板

这个模板为我自己编写的。

## Usage

- 使用 VSCode 中的插件 `LaTeX Workshop` 进行编译，在插件 `命令 -> 构建 LaTeX 项目 -> 配方: xelatex -> bibtex -> xelatex * 2` 来构建，这种构建方案会把完整的 PDF 呈现出来。
- 在只写文本，不管引用时，可以直接使用 `XeLaTeX` 来编译，速度会快一点。