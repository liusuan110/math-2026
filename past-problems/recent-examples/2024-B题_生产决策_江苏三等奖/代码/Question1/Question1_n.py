# 定义参数
p0 = 0.10  # 标称次品率
p = 0.15  # 抽样次品率
z = 1.645  # 拒收临界值

# 计算n的值
n = (p0 * (1 - p0) * z**2) / ((p - p0)**2)
print(n)

# 定义参数
p0 = 0.10  # 标称次品率
p = 0.15  # 抽样次品率
z = -1.282  # 拒收临界值

# 计算n的值
n = (p0 * (1 - p0) * z**2) / ((p - p0)**2)
print(n)
