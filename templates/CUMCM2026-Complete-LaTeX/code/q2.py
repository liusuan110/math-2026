"""Placeholder for problem 2.

Replace this file with the optimization, prediction, or simulation code used
for the second question.
"""

from __future__ import annotations


def solve_q2(params: dict[str, float]) -> tuple[dict[str, float], float]:
    """Return a placeholder solution and score."""
    solution = {"x": params.get("x0", 0.0)}
    score = 0.0
    return solution, score


if __name__ == "__main__":
    print(solve_q2({"x0": 1.0}))
