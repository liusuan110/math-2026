//==============================================================
//  CUMCM 2026 论文骨架（半成品 · Typst）—— 赛时直接在此填空
//  用法：复制本文件为 main.typ，改下面 cumcm.with(...) 的队伍信息，逐节填内容。
//  本地编译（在 cumcm-typst 目录下）：
//    typst compile --font-path template/fonts template/论文骨架.typ --root .
//
//  ★ 2026 规范已核对：正文≤30页；摘要≤1页含关键词无需英文；不要目录；
//    页码从摘要页起阿拉伯数字居中（模板已处理）。
//    电子版提交：把 cover-display 改为 false（去承诺书+编号页，第一页即摘要）。
//==============================================================
#import "../lib.typ": *
#show: thmrules

#show: cumcm.with(
  title: "【在此填论文标题】",
  problem-chosen: "C",            // 题号 A/B/C/D/E
  team-number: "【12位队号】",
  college-name: "【学校全称，不含院系】",
  member: (
    A: "【队员一】",
    B: "【队员二】",
    C: "【队员三】",
  ),
  advisor: "【指导教师，可留空】",
  date: datetime(year: 2026, month: 9, day: 10),

  cover-display: true,            // 电子版提交改为 false（去封面与编号页）

  // 摘要：评委第一眼，权重最高。≤1页，每问有量化结论。
  // 套路：背景一句 → 问题N用了什么模型/方法 → 关键结果(带数字) → 结论/亮点
  abstract: [
    针对【问题背景一句话概括】，本文【总体思路：建立了…模型 / 采用了…方法】，解决了【N】个问题。

    *针对问题一*，【方法/模型】，【求解手段】，得到【关键结果，务必带具体数字与单位】。

    *针对问题二*，【方法/模型】，【求解手段】，得到【关键结果，带数字】。

    *针对问题三*，【方法/模型】，【求解手段】，得到【关键结果，带数字】。

    最后，本文对模型进行了【灵敏度分析/稳健性检验】，结果表明【模型对关键参数的敏感程度结论】。本文的亮点在于【创新点一句话】。
  ],
  keywords: ("关键词1", "关键词2", "关键词3", "关键词4"),
)

= 问题重述
== 问题背景
【用自己的话复述背景，不要照抄题目，2~3 句即可。】

== 问题提出
本文需要解决以下问题：
#list(
  [*问题一：*【一句话概括】],
  [*问题二：*【一句话概括】],
  [*问题三：*【一句话概括】],
)

= 问题分析
【总体分析：数据/对象的特点，整体思路；可放一张总体技术路线流程图。】

== 问题一的分析
【属于 评价/优化/预测/机理 哪一类，为什么选这个模型，思路链条。】

== 问题二的分析
【同上。】

== 问题三的分析
【同上。】

= 模型假设
#enum(
  [假设题目所给数据真实可靠，不考虑统计与测量误差对结论的根本性影响；],
  [假设【研究对象】在研究时段内不受突发外部事件（政策、灾害等）影响；],
  [假设【关键变量】的变化连续/平稳，可用【对应数学工具】刻画；],
  [【按题目补充 1~3 条与本题强相关、且后文用得上的假设】。],
)

= 符号说明
#figure(
  table(
    columns: 2,
    align: (center, left),
    stroke: none,
    table.hline(),
    table.header([*符号*], [*意义*]),
    table.hline(stroke: 0.6pt),
    [$x_i$], [【第 $i$ 个决策变量 / 含义】],
    [$w_j$], [【第 $j$ 个指标的权重】],
    [$hat(y)_t$], [【$t$ 时刻的预测值】],
    [$epsilon$], [【误差项 / 容差】],
    table.hline(),
  ),
  caption: "符号说明",
)
注：其余符号在首次出现处说明。

= 问题一模型的建立与求解
== 模型建立
【数据预处理（缺失/异常/归一化）→ 建立模型，写清目标函数、约束、公式。】
$ max Z = sum_(i=1)^n c_i x_i $
约束条件：
$ "s.t." quad sum_(i=1)^n a_(i j) x_i <= b_j, quad j=1,2,...,m $

== 模型求解
【说明用什么算法/工具求解（对应 code/ 里的模块），给出求解流程。】

== 结果分析
【把结果用图表呈现并解读，给出明确的量化结论。】
// #figure(image("./figures/q1_result.png", width: 70%), caption: "问题一结果")

= 问题二模型的建立与求解
== 模型建立
【同问题一结构。】
== 模型求解
== 结果分析
【结论带数字。】

= 问题三模型的建立与求解
== 模型建立
【复制问题二结构填写。】
== 模型求解
== 结果分析

= 模型的检验与灵敏度分析
【对关键参数做扰动，观察结果变化（对应 code/common/sensitivity.py）。】
【结论模板：当参数 X 在 ±10% 范围变化时，最优解/预测值变化不超过 Y%，说明模型稳健。】
#figure(
  table(
    columns: 3,
    align: center,
    stroke: none,
    table.hline(),
    table.header([参数变化], [输出值], [变化率]),
    table.hline(stroke: 0.6pt),
    [$-10%$], [【】], [【】],
    [基准], [【】], [$0$],
    [$+10%$], [【】], [【】],
    table.hline(),
  ),
  caption: "关键参数灵敏度分析",
)

= 模型的评价、改进与推广
== 模型的优点
#enum(
  [【模型贴合实际，考虑了……（针对性优点）】；],
  [【方法成熟可靠，结果可解释/可复现】；],
  [【对数据规模/噪声有较好适应性】。],
)
== 模型的缺点
#enum(
  [【做了……简化假设，可能与实际有偏差】；],
  [【对……参数较敏感 / 计算复杂度较高】。],
)
== 模型的改进与推广
【如何放松假设/换更强的方法；本模型还能用于哪些类似场景。】

// 参考文献（在 refs.bib 中维护条目，正文用 #cite(<key>) 引用）
#bib(bibliography("refs.bib"))

#pagebreak()
// 附录：完整代码（页数不限，务必能与正文结果对上）
#appendix("问题一求解代码（Python）",
```py
# 在此粘贴问题一的完整 Python 代码
import numpy as np
print("hello, CUMCM 2026")
```)

#appendix("问题二求解代码（Python）",
```py
# 在此粘贴问题二的完整 Python 代码
```)
